apiVersion: v1
kind: Service
metadata:
  name: heartbeat-api
  namespace: heartbeat
spec:
  type: ClusterIP
  selector:
    app: heartbeat-api
  ports:
    - name: http
      port: 80
      targetPort: http
